[project]
name = "rcmdp-lf"
version = "0.1.0"
description = ""
authors = []
requires-python = "~=3.9"
readme = "README.md"
dependencies = [
    "numpy>=1.24.1,<2",
    "jax>=0.4.1,<0.5",
    "seaborn>=0.12.2,<0.13",
    "matplotlib>=3.6.3,<4",
    "jaxlib>=0.4.1,<0.5",
    "tqdm>=4.64.1,<5",
    "chex>=0.1.6,<0.2",
    "notebook>=7.2.1,<8",
    "jaxopt>=0.8.3,<0.9",
]

[tool.hatch.build.targets.sdist]
include = ["rcmdp_lf"]

[tool.hatch.build.targets.wheel]
include = ["rcmdp_lf"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
